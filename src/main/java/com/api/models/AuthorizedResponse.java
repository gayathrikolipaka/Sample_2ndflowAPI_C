package com.api.models;

public class AuthorizedResponse {
    // The response body is an empty JSON object ({}), so no fields are defined.
    // This class exists to allow for type-safe deserialization and future extensibility.

    public AuthorizedResponse() {
    }
}